allSkus <- read.csv("AgrotechSKUs.csv", row.names = 1)
skus <- allSkus[, c(2, 10, 11)]
View(allSkus)
View(skus)
barplot(table(skus$CriticalCode))
?barplot
?par
table(skus$CriticalCode)
?table
table(skus$CriticalCode, useNA = "always")
barplot(table(skus$CriticalCode, useNA = "always"))
summary(skus$TimiLianikis)
table(skus$TimiLianikis)
?cut
?cut2
??cut2
library(Hmisc)
priceRange <- cut(skus$TimiLianikis, breaks = 10)
priceRange
summary(priceRange)
allSkus <- read.csv("AgrotechSKUs.csv", row.names = 1)
skus <- allSkus[, c(2, 10, 11)]
# timi <- skus$TimiLianikis
# zitisi <- skus$PosotitaPoliseon
# critical <- skus$CriticalCode
# library(ggplot2)
# xyplot(zitisi ~ timi | critical, data = skus)
View(allSkus)
View(skus)
plot(skus$TimiLianikis, skus$PosotitaPoliseon, col = skus$CriticalCode)
plot(log(skus$TimiLianikis), skus$PosotitaPoliseon, col = skus$CriticalCode)
plot(log(skus$TimiLianikis), log(skus$PosotitaPoliseon), col = skus$CriticalCode)
table(skus$Cri)
skus$CriticalCode
summary(skus$CriticalCode)
View(allSkus)
View(skus)
